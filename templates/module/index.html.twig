{% extends 'base.html.twig' %}

{% block title %}Liste des modules{% endblock %}

{% block body %}

<h1>Liste des modules</h1>

<table class="table table-bordered">
    <thead>
        <tr>
            <th>Titre</th>
            <th>Catégorie</th>
        </tr>
    </thead>
{% for module in modules %}
    <tr scope="col">
        <td scope="'row"><a href=" {{ path('show_module', {'id':module.id}) }} "> {{ module.titre }} </a></td>
        <td scope="'row"> {{ module.categorie.titre }} </td>
    </tr>
{% endfor %}
</table>

<p><a href="{{ path('new_module') }}">Ajouter un module</a></p> <br>

{% endblock %}